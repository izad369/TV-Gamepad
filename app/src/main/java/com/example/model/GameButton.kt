package com.example.model

enum class GameButton {
    A, B, X, Y,
    Z,
    UP, DOWN, LEFT, RIGHT,
    D2_UP, D2_DOWN, D2_LEFT, D2_RIGHT,
    L1, L2, R1, R2,
    L3, R3,
    SELECT, START, HOME, MENU,
    UNKNOWN
}
